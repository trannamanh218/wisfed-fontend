import BookShelves from 'pages/shelves';

export default {
	title: 'Pages/Shelves page',
	component: BookShelves,
};

const Template = () => <BookShelves />;

export const Default = Template.bind({});
