import QuoteDetail from 'pages/quote-detail';

export default {
	title: 'Pages/ Quote Detail Page',
	component: QuoteDetail,
};

const Template = () => <QuoteDetail />;

export const Default = Template.bind({});
