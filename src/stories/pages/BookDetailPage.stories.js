import BookDetail from 'pages/book-detail';

export default {
	title: 'Pages/Book Detail Page',
	component: BookDetail,
};

const Template = () => <BookDetail />;

export const Default = Template.bind({});
